CREATE TABLE `curso_php`.`cliente` (
  `id` INT NOT NULL,
  `nome` VARCHAR(100) NULL,
  `email` VARCHAR(255) NULL,
  `endereco` VARCHAR(255) NULL,
  `doc` VARCHAR(45) NULL,
  `importancia` INT NULL,
  PRIMARY KEY (`id`));